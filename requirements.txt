# Kernabhängigkeiten
Django==4.2.16
gunicorn==20.1.0
whitenoise==6.5.0

# Datenbank
dj-database-url==2.2.0
psycopg2-binary==2.9.9

# PyTorch (CPU-Version)
--find-links https://download.pytorch.org/whl/cpu
torch==2.4.1

# Wissenschaftliche Bibliotheken (Minimal-Set)
numpy==1.24.3
pandas==1.5.3

# Andere essenzielle Abhängigkeiten
requests==2.31.0
python-dateutil==2.8.2
pytz==2023.3

# Fügen Sie hier andere spezifische Abhängigkeiten hinzu, die Ihre Anwendung unbedingt benötigt